services:
  - type: web
    name: csv-cleaner-corva
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web.web_app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
